{
  "buildCommand": "pip install -r requirements.txt",
  "devCommand": "python app.py",
  "framework": "python",
  "outputDirectory": ".",
  "env": {
    "SECRET_KEY": "@golf_secret"
  },
  "envPrefix": "GOLF_",
  "installCommand": "pip install -r requirements.txt",
  "functions": {
    "api/index.py": {
      "memory": 1024,
      "maxDuration": 30
    }
  }
}
